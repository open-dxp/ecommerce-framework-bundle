services:
    _defaults:
        public: false
        autowire: true
        autoconfigure: true

    #
    # COMMANDS
    #

    # auto-register all commands as services
    OpenDxp\Bundle\EcommerceFrameworkBundle\Command\:
        resource: '../../Command'
        tags: ['console.command']
